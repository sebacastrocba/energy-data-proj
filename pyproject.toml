[project]
name = "pda-project"
version = "0.1.0"
description = ""
authors = [
    {name = "sebacastrocba",email = "scastro@unc.edu.ar"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "pda_project", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
